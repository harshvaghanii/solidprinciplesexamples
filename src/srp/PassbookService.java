package srp;

public class PassbookService {

    public void printPassBook(User user) {
        String accountNumber = user.getAccNumber();
//        Do some work to print the passbook for the user
    }
}
